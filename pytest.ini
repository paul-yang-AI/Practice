[pytest]
markers =
    unit: L1 deterministic unit tests (zero LLM, zero network)
    integration: L2 mock integration tests
    eval: L3 eval harness (slow, uses cache)
testpaths = tests
pythonpath = .

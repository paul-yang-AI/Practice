"""Eval runner — full CSV export on Day 4."""

from __future__ import annotations


def run_eval(split: str = "train", output_dir: str = "reports") -> str:
    """Placeholder until Phase 1 Day 4."""
    return f"{output_dir}/eval_stub.csv"
